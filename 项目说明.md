# 桃子味🍑影院 — 项目说明

## 一、项目是做什么的

一个基于 PHP + AmazeUI 的免费在线影视站，数据源全部来自 360kan（360影视）的公开 JSON 接口，提供电影、电视剧、动漫、综艺的搜索 / 列表 / 详情 / 播放，以及电视直播和 VIP 视频解析功能。无后台、无数据库，纯前端页面 + 接口采集。

## 二、目录结构

```
/workspace
├── index.php          首页（banner 轮播 + 电影/电视剧/动漫/综艺列表 + 热播榜）
├── movie.php          电影列表页（筛选）
├── tv.php             电视剧列表页（筛选）
├── cartoon.php        动漫列表页（筛选）
├── variety.php        综艺列表页（筛选）
├── search.php         搜索页
├── player.php         播放页入口（按参数分派到下方 inc 详情模块）
├── live.php           电视直播页（分组频道 + hls.js 播放）
├── live_proxy.php     直播流同源代理（解决 m3u8 跨域/防盗链）
├── vip.php            VIP 视频解析页（外挂解析接口）
├── config.php         站点配置（站点名、视频解析接口 videoapi）
├── init.php           公共入口（加载配置 + functions + ui）
├── assets/            前端资源（AmazeUI、hls.js、css、字体）
├── data/              静态数据（live_verified.txt 直播源验证缓存）
└── inc/
    ├── functions.php  核心：所有接口封装函数（见下方接口清单）
    ├── ui.php         公共 UI：页面头/导航/页脚/影片卡片 movieItem
    ├── movie.php      电影播放详情页
    ├── tv.php         电视剧播放详情页
    ├── ct.php         动漫播放详情页
    └── va.php         综艺播放详情页
```

## 三、接口清单（全部封装在 inc/functions.php）

| 函数 | 调用的接口 | 用途 |
|------|-----------|------|
| `filterList($catid,$opts)` | `api.web.360kan.com/v1/filter/list` | 分类列表（1电影/2电视剧/3综艺/4动漫），支持 cat/area/year/act 筛选 |
| `rankList($cat)` | `api.web.360kan.com/v1/rank` | 热播榜（2电影/3电视剧/4综艺/5动漫） |
| `homeBanner()` | `api.web.360kan.com/v1/block?blockid=522` | 首页轮播 banner（360kan"猜你喜欢"推荐） |
| `searchVideos($kw)` | `api.so.360kan.com/index` | 关键词搜索 |
| `playInfo($cat,$id)` | `api.web.360kan.com/v1/play` | 播放信息（返回各播放源链接） |
| `detailInfo($cat,$id)` | `api.web.360kan.com/v1/detail` | 影片详情 |
| `episodeList($cat,$ent,$site)` | `api.so.360kan.com/episodesv2` | 剧集列表（电视剧/动漫） |
| `varietyEpisodes(...)` | `api.web.360kan.com/v1/detail` + site | 综艺剧集列表 |
| `getMovieLinks(...)` | 内部组合 playInfo | 生成可播放链接 |
| `curl($url)` | 通用 | 所有 HTTP 请求底层封装 |

### 非 360kan 接口

| 页面 | 数据来源 | 位置 |
|------|---------|------|
| 电视直播 | 直播源 TXT：`live.zbds.top/tv/iptv4.txt` | `liveSource()` functions.php:394 |
| 直播源验证 | 本地 `data/live_verified.txt`（已过滤无效频道） | `liveSource()` 优先读取 |
| VIP 解析 | 第三方解析接口（优酷/爱奇艺/腾讯等） | `config.php` 的 `videoapi` 配置 |

## 四、页面与接口对应关系

| 页面 | 用到的函数 |
|------|-----------|
| index.php | `homeBanner()` `filterList()` `rankList()` |
| movie.php / tv.php / cartoon.php / variety.php | `filterList()` |
| search.php | `searchVideos()` |
| player.php -> inc/movie.php | `playInfo()` `getMovieLinks()` |
| player.php -> inc/tv.php / inc/ct.php | `playInfo()` `episodeList()` |
| player.php -> inc/va.php | `playInfo()` `varietyEpisodes()` |
| live.php | `liveSource()` `parseLiveChannels()` |
| vip.php | `config.php` 中 `videoapi` 列表 |

## 五、某个页面不行怎么替换接口

### 1. 首页 / 分类列表 / 榜单不显示数据

`filterList()` 和 `rankList()` 依赖 `api.web.360kan.com`，接口失效时页面列表为空。替换方法：

修改 `inc/functions.php` 中 `filterList()`（约 94 行）和 `rankList()`（约 122 行）里的接口地址，换成同格式的新接口（要求返回 `data.movies` / `data` 数组）：

```php
function filterList($catid, $opts = array()) {
    ...
    $url = 'https://你的新接口/filter/list?' . http_build_query($params);
    ...
}
```

返回字段名需对齐：`id`（影片ID）、`title`（标题）、`cover`（封面）、`cdncover`（CDN封面，推荐用）、`moviecategory`（分类）、`score`（评分）、`pubdate`（年代）、`upinfo`（更新集数）。字段名不同时，改 `index.php` / 列表页里取用的 key 即可。

### 2. 首页 banner 不显示

`homeBanner()` 用 `blockid=522`。替换：改 functions.php 约 471 行接口地址；或改用一个可用的 JSON 接口，返回数组每项含 `img` / `name` / `description` / `url`：

```php
function homeBanner($cacheTtl = 21600) {
    ...
    $data = api_get('https://你的新接口/banner');
    // 输出格式：array(array('img'=>, 'name'=>, 'description'=>, 'url'=>))
}
```

### 3. 电影播放失败 / 无播放源

`playInfo()` + `getMovieLinks()` 组合决定可播放链接。替换：改 `playInfo()` 接口（约 146 行），或调 `config.php` 的 `videoapi`（VIP 解析接口）兜底——播放页可直接切解析接口。若接口返回结构不同，调整 `getMovieLinks()` 的解析逻辑。

### 4. 电视直播没有频道 / 播放不了

`liveSource()` 默认抓 `https://live.zbds.top/tv/iptv4.txt`。替换为其他 TXT 直播源：

```php
function liveSource($url = 'https://你的新直播源.txt', $cacheTtl = 21600)
```

注意：
- 若新源含失效频道，可重新跑一遍验证流程，把有效频道写入 `data/live_verified.txt`（`liveSource()` 优先读取它）。
- 格式要求为 TXT：分组行 `分组名,#genre#`，频道行 `频道名,http://...m3u8`。
- 直播播放经由 `live_proxy.php` 同源代理转发，一般无需改动。

### 5. VIP 解析不可用

改 `config.php` 的 `videoapi` 数组，替换为新的解析接口地址（name + url），页面会自动列出所有可切换。

## 六、接口返回格式约定（360kan）

- 列表接口：`data.movies`（数组），`data.total`
- banner：`data.lists`（数组，含 `title` / `comment` / `pic_lists[].url` / `cat` / `ent_id`）
- 详情：`data`（含 `playlinks`、`sites` 等）
- 榜单：`data`（数组）

所有数据经 `curl()` 抓取、`api_get()` JSON 解码；有缓存的地方用系统临时目录（`sys_get_temp_dir()`）存 JSON，默认缓存 6 小时（`$cacheTtl = 21600`）。
